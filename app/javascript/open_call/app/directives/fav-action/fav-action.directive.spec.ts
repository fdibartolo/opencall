import { FavActionDirective } from './fav-action.directive';

describe('FavActionDirective', () => {
  it('should create an instance', () => {
    const directive = new FavActionDirective();
    expect(directive).toBeTruthy();
  });
});
