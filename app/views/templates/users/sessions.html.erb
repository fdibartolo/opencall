<div ng-cloak ng-init="sessions()" fade-animation="true" class="row">
  <div ng-if="loading" class="loading">
    <p class="text-xs-center"><span class="fa fa-spinner fa-pulse"></span></p>
  </div>
  <div ng-if="!loading">
    <div ng-if="sessions.length == 0" class="no-search-results">
      <h2 class="text-xs-center">
        <%= t('sessions.labels.no_sessions_yet') %><br><br><br><br>
        <div class="add-new-session"><span class="fa fa-arrow-down"></span></div>
      </h2>
      <div class="action-button-container">
        <a class="img-circle btn-action primary" href="<%= root_url + '#/sessions/new' %>">
          <span class="fa fa-pencil icon" aria-hidden="true"></span>
        </a>
      </div>
    </div>
    <div ng-if="sessions.length > 0" class="col-sm-offset-1 col-sm-10"><br>
      <h2 class="text-xs-center"><%= t('sessions.labels.my_sessions') %></h2><br>
      <div ng-repeat="session in sessions" class="session-summary" fade-animation="true">
        <h3>
          <a ng-href="#/sessions/show/{{session.id}}">{{session.title}}</a>
          <a ng-href="#/sessions/edit/{{session.id}}" class="btn btn-sm btn-success pull-xs-right" style="color:white;margin:10px">
            <%= t('sessions.buttons.edit_session') %> &nbsp; 
            <span class="fa fa-pencil" aria-hidden="true"></span>
          </a>
        </h3>
        <div class="track">
          <a class="video-link" ng-href="{{session.video_link}}" target="_blank" ng-if="session.video_link != null && session.video_link != '' && session.video_link != '-'" tooltip='<%= "top|#{t('sessions.labels.video')}" %>'>
            <span class="fa fa-video-camera">&nbsp;</span>
          </a>
          {{session.theme}} - {{session.track}}
        </div>
        <p markdown="session.summary"></p>
        <div>
          <ul class="list-inline">
            <li ng-repeat="tag in session.tags track by $index" class="tag sm bg-primary">{{tag}}</li>
          </ul>
        </div>
      </div>
      <div style="margin-top:100px"></div>
      <div id="bottom-fade"></div>
    </div>
  </div>
</div>
