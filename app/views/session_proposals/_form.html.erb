<%= form_for(@session_proposal) do |f| %>
  <% if @session_proposal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@session_proposal.errors.count, "error") %> prohibited this session_proposal from being saved:</h2>

      <ul>
      <% @session_proposal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :author %><br>
    <%= f.text_field :author %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
